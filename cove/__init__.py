"""Cove — a self-hosted media-center-style downloader.

Runs on the user's PC, writes finished files to a Synology NAS, and serves a
React GUI (built into web/dist) plus a JSON + WebSocket API from FastAPI.
"""

__version__ = "0.1.0"
